#lang sicp
(define (product term a next b)
  (if (> a b)
      0
      (* (term a) (product term (next a) next b))))
(define f(x) x)
(define (inc n) (+ n 1))
(define (fact n)
  (product f 1 inc 6))

(fact 6)